services:
  - type: web
    name: TrustGuardAI-backend
    runtime: python
    rootDir: backend
    branch: master
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host=0.0.0.0 --port=$PORT
